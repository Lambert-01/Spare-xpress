SITE_NAME="SPARE XPRESS LTD"
SITE_URL="https://your-domain.com"
SITE_EMAIL="support@your-domain.com"
SITE_PHONE="+250 792 865 114"
SITE_ADDRESS="Kagarama, Kicukiro, Kigali, Rwanda"

# MySQL connection. Use these values for a normal VPS MySQL/MariaDB database.
DB_HOST="localhost"
DB_PORT="3306"
DB_NAME="sparedb"
DB_USER="spareuser"
DB_PASS="change-this-password"

# Alternative MySQL URL format, if your host provides one.
# DATABASE_URL="mysql://spareuser:change-this-password@localhost:3306/sparedb"

# Neon gives PostgreSQL URLs. This app currently uses mysqli/MySQL, so a Neon URL
# will not work until the database layer and SQL schema are migrated to PostgreSQL.
# DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM_EMAIL=""
SMTP_FROM_NAME="SPARE XPRESS LTD"

CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
